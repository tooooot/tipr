# 🚀 دليل البدء السريع - التداول الفعلي

## الخطوة 1: إنشاء حساب Binance Testnet (للاختبار)

1. اذهب إلى: **https://testnet.binance.vision/**
2. **اضغط على "Generate HMAC_SHA256 Key"**
3. **احفظ الـ API Key و Secret Key**

> ⚠️ هذا حساب تجريبي - بيانات وهمية للاختبار فقط!

---

## الخطوة 2: تثبيت المكتبات المطلوبة

```bash
cd c:\Users\HP\Desktop\Apps\Tipr
pip install -r requirements_trading.txt
```

---

## الخطوة 3: إعداد ملف `.env`

```bash
# انسخ ملف المثال
cp .env.example .env

# ثم افتح .env وعدّل القيم:
```

**محتوى ملف `.env`:**
```env
BINANCE_API_KEY=YOUR_TESTNET_API_KEY_HERE
BINANCE_SECRET_KEY=YOUR_TESTNET_SECRET_KEY_HERE
TRADE_MODE=TEST
MAX_POSITION_SIZE=50
MAX_OPEN_TRADES=3
MAX_DAILY_LOSS=100
```

---

## الخطوة 4: تشغيل الروبوت

```bash
python live_trading_engine.py
```

**المتوقع أن ترى:**
```
======================================================================
🤖 TIPR LIVE TRADING ENGINE
======================================================================
🧪 Paper Trading
💰 أقصى قيمة للصفقة: $50.0
📊 أقصى عدد صفقات: 3
🛑 أقصى خسارة يومية: $100.0
======================================================================
✅ الاتصال ناجح
💵 الرصيد: $10000.00 USDT
```

---

## الخطوة 5: اختبار لمدة أسبوعين

- ✅ راقب أداء الروبوت يومياً
- ✅ سجل عدد الصفقات الناجحة/الخاسرة
- ✅ احسب نسبة الربح الإجمالية

---

## الخطوة 6: الانتقال للتداول الحقيقي ⚠️

**فقط إذا كنت واثقاً 100% من النتائج:**

1. **أنشئ API Key من حساب Binance الحقيقي:**
   - https://www.binance.com/en/my/settings/api-management
   - ✅ فعّل: Enable Reading, Enable Spot Trading
   - ❌ لا تفعّل: Enable Withdrawals

2. **عدّل ملف `.env`:**
   ```env
   TRADE_MODE=LIVE
   MAX_POSITION_SIZE=20  # ابدأ بمبلغ صغير!
   ```

3. **شغّل الروبوت:**
   ```bash
   python live_trading_engine.py
   ```

---

## 📊 مراقبة الأداء

الملفات المهمة:
- `live_trades_data.json` - سجل جميع الصفقات
- `frontend/src/data/real_trades.json` - للعرض في الواجهة

---

## 🆘 حل المشاكل الشائعة

### ❌ "Invalid API Key"
- تأكد من نسخ API Key و Secret بشكل صحيح
- إذا كنت تستخدم Testnet، استخدم مفاتيح Testnet فقط

### ❌ "Insufficient Balance"
- في Testnet، الرصيد الافتراضي محدود
- قلل `MAX_POSITION_SIZE` في `.env`

### ❌ "Unknown order sent"
- المشكلة في دقة الكمية (quantity precision)
- الكود يحاول التعامل معها تلقائياً

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع ملف `LIVE_TRADING_GUIDE.md` للتفاصيل الكاملة
2. تحقق من Binance Docs: https://binance-docs.github.io/
3. اسألني! 😊

---

## ⚠️ تذكير أخير

- **لا تستثمر أكثر مما تستطيع خسارته**
- **اختبر دائماً في Testnet أولاً**
- **راقب الروبوت بشكل يومي**
- **ابدأ بمبالغ صغيرة جداً**

**حظاً موفقاً! 🚀**
