# 🔑 دليل إنشاء API Key من حساب Binance الحقيقي

## ⚠️ مهم جداً - اقرأ هذا أولاً!

- ✅ هذا دليل للتداول **الحقيقي** بأموال حقيقية
- ✅ 10 دولار مبلغ مناسب للتجربة
- ⚠️ **لا تفعّل صلاحية السحب** (Withdrawal) أبداً
- ⚠️ احتفظ بمفاتيح API في مكان آمن

---

## الخطوات:

### 1️⃣ تسجيل الدخول إلى Binance

اذهب إلى: **https://www.binance.com**

### 2️⃣ الذهاب إلى إعدادات API

1. اضغط على **أيقونة الملف الشخصي** (أعلى اليمين)
2. اختر **API Management** أو **إدارة API**
3. أو اذهب مباشرة إلى: https://www.binance.com/en/my/settings/api-management

### 3️⃣ إنشاء API Key جديد

1. اضغط على **"Create API"** أو **"إنشاء API"**
2. اختر **"System Generated"** (النظام يولّد المفتاح)
3. أدخل **Label** (اسم تعريفي): مثل `Tipr Trading Bot`
4. أكمل التحقق الأمني (2FA code)

### 4️⃣ حفظ المفاتيح (مهم جداً! 🔴)

بعد الإنشاء ستظهر لك:
- **API Key** - سلسلة طويلة من الأحرف
- **Secret Key** - سلسلة أخرى (تظهر مرة واحدة فقط!)

⚠️ **انسخهم الآن وخزّنهم في مكان آمن!**

```
API Key: 
[انسخه هنا مؤقتاً]

Secret Key:
[انسخه هنا مؤقتاً - لن تراه مرة أخرى!]
```

### 5️⃣ تعيين الصلاحيات الصحيحة

بعد الإنشاء، اضبط الصلاحيات:

**✅ فعّل هذه:**
- ✅ **Enable Reading** - قراءة البيانات
- ✅ **Enable Spot & Margin Trading** - التداول الفوري

**❌ لا تفعّل هذه:**
- ❌ **Enable Withdrawals** - السحب (خطر أمني!)
- ❌ **Enable Futures** - العقود الآجلة (غير مطلوب)
- ❌ **Enable Internal Transfer** - التحويل الداخلي

### 6️⃣ تقييد IP Address (موصى به للأمان)

**الخيار 1: Unrestricted (غير مقيد)**
- يعمل من أي IP
- ⚠️ أقل أماناً لكن أسهل

**الخيار 2: Restrict access to trusted IPs (موصى به)**
1. معرفة IP الخاص بك: اذهب إلى https://whatismyip.com
2. أدخل IP الخاص بك في القائمة
3. الآن المفتاح يعمل فقط من جهازك

**للبداية:** اختر **Unrestricted** لسهولة الاختبار، لكن غيّره لاحقاً

### 7️⃣ حفظ المفاتيح في ملف `.env`

في مجلد Tipr، انسخ ملف `.env.example` إلى `.env`:

```powershell
cd c:\Users\HP\Desktop\Apps\Tipr
cp .env.example .env
```

ثم افتح `.env` وضع مفاتيحك:

```env
# === Binance API Keys (حساب حقيقي) ===
BINANCE_API_KEY=paste_your_api_key_here
BINANCE_SECRET_KEY=paste_your_secret_key_here

# === وضع التداول ===
TRADE_MODE=LIVE

# === إعدادات مخصصة لرأس مال 10 دولار ===
MAX_POSITION_SIZE=2        # صفقة واحدة = 2 دولار فقط
MAX_OPEN_TRADES=2          # أقصى صفقتين مفتوحة
MAX_DAILY_LOSS=3           # أقصى خسارة يومية = 3 دولار

# === إدارة المخاطر ===
DEFAULT_STOP_LOSS_PCT=0.02     # وقف خسارة 2%
DEFAULT_TAKE_PROFIT_PCT=0.04   # هدف ربح 4%
```

### 8️⃣ التحقق من الاتصال

بعد إعداد `.env`، شغّل:

```powershell
python live_trading_engine.py
```

**يجب أن ترى:**
```
======================================================================
🤖 TIPR LIVE TRADING ENGINE
======================================================================
🔴 LIVE TRADING
💰 أقصى قيمة للصفقة: $2.0
📊 أقصى عدد صفقات: 2
🛑 أقصى خسارة يومية: $3.0
======================================================================
✅ الاتصال ناجح
💵 الرصيد: $10.00 USDT
```

---

## 🛡️ نصائح الأمان

### ✅ افعل:
1. احفظ المفاتيح في مكان آمن (password manager)
2. فعّل 2FA على حسابك في Binance
3. راقب الصفقات يومياً
4. ابدأ بمبالغ صغيرة جداً (2 دولار)

### ❌ لا تفعل:
1. لا تشارك المفاتيح مع أحد
2. لا ترفع ملف `.env` على GitHub
3. لا تفعّل صلاحية السحب
4. لا تترك الروبوت يعمل بدون مراقبة

---

## 📊 التوقعات الواقعية

**برأس مال 10 دولار:**
- صفقة واحدة = 2 دولار
- ربح متوقع لكل صفقة ناجحة: ~4% = **0.08 دولار**
- 5 صفقات ناجحة = **+0.40 دولار**
- هدف شهري واقعي: **+10-20%** = 1-2 دولار

**الهدف الحقيقي:**
- ✅ اختبار الروبوت
- ✅ تعلم كيف يعمل
- ✅ بناء الثقة في النظام
- ❌ **ليس الثراء السريع!**

---

## 🚀 جاهز للبدء؟

بعد إعداد كل شيء:

```powershell
# تثبيت المكتبات (مرة واحدة)
pip install -r requirements_trading.txt

# تشغيل المحرك
python live_trading_engine.py
```

---

## 📞 إذا واجهت مشاكل:

### "Invalid API Key"
- تأكد من نسخ API Key بشكل صحيح (بدون مسافات)
- تأكد من تفعيل صلاحية Trading

### "Insufficient Balance"
- تأكد أن لديك USDT في Spot Wallet
- إذا كان رصيدك بعملة أخرى، حوّله إلى USDT

### "IP Restricted"
- إذا قيّدت IP، تأكد أن IP جهازك مطابق
- أو اختر Unrestricted

---

**حظاً موفقاً! 🚀**
